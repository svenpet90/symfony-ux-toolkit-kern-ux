{# @prop as 'h5' The HTML tag to render. Defaults to `h2` #}
{# @block content The title text of the card #}
{%- props as = 'h2' -%}

<{{ as }} class="kern-title {{ attributes.render('class') }}" {{ attributes }}>
{%- block content %}{% endblock -%}
</{{ as }}>