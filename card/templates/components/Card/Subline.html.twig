{# @prop as 'p' The HTML tag to render. Defaults to `p` #}
{# @block content The Sibline text of the card #}
{%- props as = 'p' -%}

<{{ as }} class="kern-subline {{ attributes.render('class') }}" {{ attributes }}>
{%- block content %}{% endblock -%}
</{{ as }}>