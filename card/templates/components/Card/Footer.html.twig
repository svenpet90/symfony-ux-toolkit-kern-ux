{# @block content The footer area, typically contains actions or additional information #}

<footer class="kern-card__footer {{ attributes.render('class') }}" {{ attributes }}>
    {%- block content %}{% endblock -%}
</footer>