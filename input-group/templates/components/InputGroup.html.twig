{# @block content An input (`kern-form-input__input`) combined with `<twig:InputGroup:Text>` add-ons and/or a `<twig:Button>` #}
<div class="kern-input-group {{ attributes.render('class') }}" {{ attributes }}>
    {%- block content %}{% endblock -%}
</div>
