{# @block content A set of `<twig:Accordion>` components rendered as a connected group #}
<div class="kern-accordion-group {{ attributes.render('class') }}" {{ attributes }}>
    {%- block content %}{% endblock -%}
</div>
